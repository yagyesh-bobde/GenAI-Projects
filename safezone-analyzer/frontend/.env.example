# Copy to .env if needed. Usually leave PUBLIC_API_URL unset: Vite proxies /api to the backend.
# Only set a full URL if port 8000 is open and you want the browser to call the API directly.
# No trailing slash.
#
# PUBLIC_API_URL=http://YOUR_VM_PUBLIC_IP:8000
